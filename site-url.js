/**
 * Site config for GitHub Pages (and local preview)
 *
 * After deploy, set SITE_URL for WhatsApp / link previews:
 *   https://YOUR-USERNAME.github.io/REPO-NAME
 *
 * Private blessings inbox — see BLESSINGS_SETUP.md:
 *   endpoint  → Google Apps Script Web App URL
 *   adminKey  → secret word; open site with ?blessings=YOUR_KEY
 *
 * Optional: copy site-url.local.js.example → site-url.local.js (gitignored)
 * to keep secrets out of a public repository.
 */
window.SITE_URL = 'https://ibm-adarsh.github.io/wedding-invitation';

window.BLESSINGS_CONFIG = {
    endpoint: '',
    adminKey: ''
};
